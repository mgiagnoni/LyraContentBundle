<a href="{{ path('admin_page_edit', { 'id' : node.id }) }}" class="button action-edit icon-only">{{ 'list.edit'|trans({}, 'LyraContentBundle') }}</a>
{% if not node.isRoot %}
<a href="{{ path('admin_content_delete', { 'id' : node.id }) }}" class="button action-delete icon-only">{{ 'list.delete'|trans({}, 'LyraContentBundle') }}</a>
{% endif %}
