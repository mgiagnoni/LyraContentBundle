{% extends "LyraContentBundle:Admin:form.html.twig" %}
{% block form_title %}
<h1>Move Content</h1>
{% endblock %}
{% block button_bar %}{% endblock %}
{% block form %}
    <p>Select a new parent for content item <em>{{ content.title }}</em> (and all its descendants):</p>
    {{ form_errors(form) }}
    <form class="ly-form" action="{{ path('lyra_content_admin_move', { 'id': content.id }) }}" method="post">
        {{ form_row(form.parent) }}
        {{ form_rest(form) }}
        <hr class="clear" />
        <input type="submit" name="submit" value="Move" />
        <a href="{{ path('lyra_content_admin_list') }}">Abort</a>
    </form>
{% endblock %}
